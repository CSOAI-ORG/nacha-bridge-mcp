# nacha-bridge-mcp

mcp-name: io.github.CSOAI-ORG/nacha-bridge-mcp

Part of the **CSOAI Layer-0 legacy-bridge family**. Bridge NACHA / ACH payment files to ONE OS — parse, validate, map, govern (NACHA rules / OFAC / BSA-AML).

**Tools:** parse_nacha · validate_nacha · map_to_modern · govern_ach

```bash
pip install -e .
python server.py
```
